# Автоматизация сбора и импорта продаж

Этот проект генерирует синтетические данные о продажах (чеки) и автоматически импортирует их в базу данных PostgreSQL. Скрипт настроен на работу по расписанию через Crontab.

## 📋 Требования

Перед запуском убедитесь, что у вас установлены:
1.  **Python 3.8+**
2.  **PostgreSQL** (локально или доступ к удаленному серверу)
3.  **Terminal** (macOS/Linux)

---

## 🚀 Установка и настройка

### Шаг 1. Подготовка файлов
1. Создайте папку проекта (например, `shop_project`).
2. Поместите туда файлы:
    *   `generate_sales.py` (Генератор чеков)
    *   `import_db.py` (Скрипт импорта в БД)
    *   `run_tasks.sh` (Скрипт-менеджер для запуска)
3. Создайте внутри папку для данных:
    ```bash
    mkdir data
    ```

### Шаг 2. Установка зависимостей
Установите драйвер для работы с PostgreSQL:
```bash
pip3 install psycopg2-binary
